# Session Note - 2026-08-14

## What we did
- Explained how the SpecKit workflow works (specify -> plan -> tasks -> implement).
- Walked through a plain-English example of SpecKit using a "check laundry status" feature.
- Listed the supporting SpecKit commands (clarify, checklist, converge, analyze, constitution).

## Decisions
- None made yet; no feature was started.

## Files touched
- None (this was a Q&A / explanation session only).

## Warnings / gotchas
- SpecKit commands are invoked as plain requests (e.g. "speckit-specify <description>"), not slash commands in this setup.
- The user is still learning the workflow; keep explanations jargon-free.

## What's left / next steps
- User may want to actually start a feature with SpecKit.
- If so: begin with `speckit-specify "<feature description>"`.
